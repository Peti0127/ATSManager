{
  "name": "outlook-mail-settings-addin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "node -e \"require('fs').copyFileSync('taskpane.html','index.html')\" && vite build && node -e \"const f=require('fs');f.unlinkSync('index.html');f.renameSync('dist/index.html','dist/taskpane.html')\""
  },
  "dependencies": {
    "@azure/msal-browser": "^4.15.0"
  },
  "devDependencies": { "vite": "^7.0.0" }
}
